# دليل تفعيل النسخ الاحتياطي على جوجل درايف + تطبيق APK

فيه 3 مراحل، لازم تتعملوا بالترتيب ده بالظبط:

1. رفع الملفات على استضافة مجانية (GitHub Pages) — عشان جوجل ومحول التطبيق يقدروا يشتغلوا مع الموقع.
2. إنشاء مفتاح Google Cloud (Client ID) وربطه بالبرنامج.
3. تحويل الموقع لملف APK باستخدام PWABuilder.

---

## المرحلة 1: رفع الملفات على GitHub Pages (استضافة مجانية)

1. روح على [github.com](https://github.com) واعمل حساب مجاني لو مش عندك.
2. اضغط **New repository**. اكتب اسم زي `dekroure-app` واختار **Public** واضغط **Create repository**.
3. جوه الريبو، اضغط **Add file → Upload files**، وارفع الملفات الخمسة دي كلها في نفس المستوى (مش جوه فولدر):
   - `index.html`
   - `manifest.json`
   - `service-worker.js`
   - `icon-192.png`
   - `icon-512.png`
4. اضغط **Commit changes**.
5. روح على **Settings → Pages** (في القائمة الجانبية).
6. تحت **Build and deployment → Branch** اختار `main` والمجلد `/ (root)`، واضغط **Save**.
7. استنى دقيقة، هيظهرلك رابط شكله كده:
   `https://اسم-حسابك.github.io/dekroure-app/`
   ← ده رابط برنامجك، احفظه.

> بعد كده أي تعديل عايز تعمله في الملف تقدر تعمله من نفس صفحة الريبو (edit) أو ترفع نسخة جديدة بنفس الأسماء.

---

## المرحلة 2: مفتاح Google Cloud (OAuth Client ID)

1. روح على [console.cloud.google.com](https://console.cloud.google.com) وسجل دخول بحساب جوجل.
2. من الأعلى، اعمل **مشروع جديد (New Project)** واسمه مثلاً `Dekroure Accounting`.
3. من القائمة الجانبية: **APIs & Services → Library**، دور على **Google Drive API** وافتحه واضغط **Enable**.
4. روح **APIs & Services → OAuth consent screen**:
   - اختار **External** ثم Create.
   - اكتب اسم التطبيق (مثلاً: نظام حسابات الدكروري) وإيميلك في الحقول المطلوبة، واحفظ.
   - في صفحة **Test users** ضيف إيميلات كل الموبايلات/الحسابات اللي هتستخدم البرنامج (تقدر تضيف لحد 100 إيميل).
5. روح **APIs & Services → Credentials → Create Credentials → OAuth client ID**:
   - Application type: **Web application**.
   - في **Authorized JavaScript origins** ضيف رابط GitHub Pages بتاعك بدون سلاش في الآخر، مثال:
     `https://اسم-حسابك.github.io`
   - اضغط **Create**.
6. هيظهرلك **Client ID** شكله شبه:
   `123456789-abcdefgh.apps.googleusercontent.com`
   انسخه.

---

## المرحلة 3: ربط الـ Client ID بالبرنامج

1. افتح رابط البرنامج بتاعك (من GitHub Pages) على الموبايل أو الكمبيوتر.
2. سجل دخول بالبرنامج، وروح لصفحة **⚙️ الإعدادات والنسخ الاحتياطي**.
3. الصق الـ Client ID في الخانة واضغط **حفظ**.
4. اضغط **تسجيل الدخول بجوجل** واختار حسابك ووافق على الصلاحيات.
5. اضغط **⬆️ رفع نسخة على درايف الآن** — هتتحفظ نسخة باسم `دكروري-نسخة-احتياطية.json` جوه Drive بتاعك.
6. من أي موبايل تاني (لازم يكون إيميله مضاف في Test users في المرحلة 2)، افتح نفس رابط GitHub Pages، سجل دخول بجوجل بنفس الحساب، واضغط **⬇️ استعادة آخر نسخة من درايف** — البيانات هتنزل وتتزامن.

> ملاحظة: تسجيل الدخول بجوجل بيشتغل بس لما البرنامج مفتوح من رابط https حقيقي (زي رابط GitHub Pages)، مش هيشتغل لو فتحت الملف مباشرة من على الجهاز.

---

## المرحلة 4: تحويل الموقع لملف APK (اختياري)

1. روح على [pwabuilder.com](https://www.pwabuilder.com)
2. الصق رابط GitHub Pages بتاعك واضغط **Start**.
3. هيفحص الموقع ويأكد إن الـ manifest والـ service worker شغالين (لازم يظهر ✅ باللون الأخضر).
4. اضغط **Package for stores → Android**.
5. سيب الإعدادات الافتراضية (أو غيّر اسم الحزمة Package ID لو حابب)، واضغط **Generate**.
6. هينزلّك ملف مضغوط جواه ملف `.apk` (وملف `.aab` لو عايز ترفعه على Google Play Store لاحقاً).
7. انسخ ملف الـ APK للموبايل وثبته مباشرة (لازم تفعّل "السماح بالتثبيت من مصادر غير معروفة" في إعدادات الموبايل)، أو استخدم ملف الـ AAB لو هترفعه على Google Play.

---

## ملخص سريع
- **بدون أي خطوات إضافية**: افتح رابط GitHub Pages من الموبايل واضغط "إضافة إلى الشاشة الرئيسية" من المتصفح — هيتحط زي تطبيق عادي فوراً.
- **مع الخطوات دي**: هيبقى عندك مزامنة تلقائية عبر جوجل درايف + ملف APK حقيقي.
